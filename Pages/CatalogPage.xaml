<Page x:Class="HeavenGarden.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HeavenGarden.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="tbSearch" Grid.Row="0" Margin="5" 
         TextChanged="tbSearch_TextChanged" 
         GotFocus="tbSearch_GotFocus" LostFocus="tbSearch_LostFocus"/>

        <ComboBox x:Name="cmbFilterCause" Grid.Row="1" Margin="5" 
          SelectionChanged="cmbFilterCause_SelectionChanged"/>

        <ListBox x:Name="lvPlants" Grid.Row="2" Margin="5" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#2E8B57" BorderThickness="1" Margin="5" Padding="5" CornerRadius="8" Background="#8FBC8F" Width="180">
                        <StackPanel>
                            <Image Source="/Images/iconPlant.png" Width="80" Height="80"/>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Species}" FontSize="12" Foreground="#2E8B57" TextAlignment="Center"/>
                            <TextBlock Text="{Binding CauseOfDeath}" FontSize="10" TextAlignment="Center"/>

                            <Button Content="Подробнее" Click="BtnDetails_Click" Tag="{Binding PlantId}" Margin="0,5,0,0" Background="#000000" Foreground="White"/>
                            <Button Content="Редактировать" Click="BtnEditPlant_Click" Tag="{Binding PlantId}" Margin="0,2,0,0" Background="#000000" Foreground="White"/>
                            <Button Content="Удалить" Click="BtnDeletePlant_Click" Tag="{Binding PlantId}" Margin="0,2,0,0" Background="Red" Foreground="White"/>
                            <Button Content="Управление статусом" Click="BtnManageStatus_Click" Tag="{Binding PlantId}" Margin="0,2,0,0" Background="#000000" Foreground="White"/>
                            <Button Content="Статистика" Click="BtnStatistics_Click" Tag="{Binding PlantId}" Margin="0,2,0,0" Background="#2E8B57" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
